import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    padding: 15,
  },
  title: {
    fontSize: 40,
    fontWeight: "bold",
  },
  language: {
    fontSize: 20,
    fontWeight: "bold",
  },
});

export default styles;
